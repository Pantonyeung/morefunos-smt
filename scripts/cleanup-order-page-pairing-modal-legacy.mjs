import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';

const DEFAULT_FILE='pages/order/page.css';
const pairingNeedles=['.specified-link-card','.pairing-group-tabs','.pairing-body','.combo-editor-card','.combo-help','.combo-role','.combo-candidates','.combo-actions','.drink-link-candidates'];

function splitTopLevelSelectors(text){
  const selectors=[];let buf='';let paren=0;let bracket=0;let quote='';
  for(let i=0;i<text.length;i+=1){const ch=text[i];if(quote){buf+=ch;if(ch===quote&&text[i-1]!=='\\')quote='';continue;}if(ch==='"'||ch==="'"){quote=ch;buf+=ch;continue;}if(ch==='(')paren+=1;else if(ch===')')paren=Math.max(0,paren-1);else if(ch==='[')bracket+=1;else if(ch===']')bracket=Math.max(0,bracket-1);if(ch===','&&paren===0&&bracket===0){selectors.push(buf.trim());buf='';}else buf+=ch;}if(buf.trim())selectors.push(buf.trim());return selectors;
}
function isPairingOwnedSelector(selector){const normalized=selector.replace(/\s+/g,' ').trim();return normalized&&pairingNeedles.some(needle=>normalized.includes(needle));}
function findMatchingBrace(css,openIndex){let depth=0,quote='',inComment=false;for(let i=openIndex;i<css.length;i+=1){const ch=css[i],next=css[i+1];if(inComment){if(ch==='*'&&next==='/'){inComment=false;i+=1;}continue;}if(quote){if(ch===quote&&css[i-1]!=='\\')quote='';continue;}if(ch==='/'&&next==='*'){inComment=true;i+=1;continue;}if(ch==='"'||ch==="'"){quote=ch;continue;}if(ch==='{')depth+=1;else if(ch==='}'){depth-=1;if(depth===0)return i;}}throw new Error(`Unbalanced CSS brace at ${openIndex}`);}
function previousBoundary(css,index){return Math.max(css.lastIndexOf(';',index-1),css.lastIndexOf('}',index-1))+1;}
function peelLeadingComments(prelude){let rest=prelude,comments='';while(true){const match=rest.match(/^(\s*\/\*[\s\S]*?\*\/)/);if(!match)break;comments+=match[1];rest=rest.slice(match[1].length);}const leadingWhitespace=rest.match(/^\s*/)?.[0]||'';comments+=leadingWhitespace;rest=rest.slice(leadingWhitespace.length);return {comments,selectorText:rest.trim()};}

export function cleanupPairingModalLegacy(css){let cursor=0,output='';let removedSelectors=0,rewrittenRules=0,removedRules=0;while(cursor<css.length){const open=css.indexOf('{',cursor);if(open===-1){output+=css.slice(cursor);break;}const boundary=previousBoundary(css,open);if(boundary<cursor){output+=css.slice(cursor,open+1);cursor=open+1;continue;}const rawPrefix=css.slice(cursor,boundary);const prelude=css.slice(boundary,open);const {comments,selectorText}=peelLeadingComments(prelude);if(selectorText.startsWith('@')){const close=findMatchingBrace(css,open);output+=css.slice(cursor,close+1);cursor=close+1;continue;}const close=findMatchingBrace(css,open);const selectors=splitTopLevelSelectors(selectorText);const kept=selectors.filter(selector=>!isPairingOwnedSelector(selector));const removed=selectors.length-kept.length;if(!removed){output+=css.slice(cursor,close+1);cursor=close+1;continue;}removedSelectors+=removed;output+=rawPrefix+comments;if(kept.length){output+=`${kept.join(',\n')} ${css.slice(open,close+1)}`;rewrittenRules+=1;}else removedRules+=1;cursor=close+1;}return {css:output,removedSelectors,rewrittenRules,removedRules};}
function assertBoundary(css){for(const needle of pairingNeedles){if(css.includes(needle))throw new Error(`Pairing Modal legacy selector remains: ${needle}`);}for(const needle of ['.link-candidates','.catalog','.detail-drinks','.status-list']){if(!css.includes(needle))throw new Error(`Non-Pairing authority removed unexpectedly: ${needle}`);}}
function main(){const args=new Set(process.argv.slice(2));const check=args.has('--check');const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const file=path.join(root,DEFAULT_FILE);const source=fs.readFileSync(file,'utf8');const result=cleanupPairingModalLegacy(source);if(source===result.css){assertBoundary(source);console.log('PAIRING_MODAL_ALREADY_CLEARED');return;}if(result.removedSelectors<12)throw new Error(`Expected at least 12 Pairing selectors, removed ${result.removedSelectors}`);if(result.removedSelectors>80)throw new Error(`Refusing broad Pairing cleanup: ${result.removedSelectors}`);assertBoundary(result.css);console.log(`PAIRING_MODAL_CLEANUP selectors=${result.removedSelectors} rules=${result.removedRules} rewritten=${result.rewrittenRules}`);if(check){process.exitCode=2;console.log('PAIRING_MODAL_CLEANUP_REQUIRED');return;}fs.writeFileSync(file,result.css,'utf8');console.log(`WROTE ${DEFAULT_FILE}`);}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url))main();
