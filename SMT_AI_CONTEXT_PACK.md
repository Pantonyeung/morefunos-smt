# 磨飯 SMT｜Chat 模式單檔接力包

用途：無法使用 Work 模式、開新 Chat、token 將盡或只可用手機時，將本文件連同最新程式 zip 上載。新對話毋須重讀舊聊天即可繼續。

## 新 Chat 第一段直接貼上

> 你現在接手「磨飯 SMT」開發。把 `SMT_AI_CONTEXT_PACK.md` 當專案入口，把最新 repository zip 當程式真相。不要用早期效果圖推翻 CURRENT／LOCKED 決策；不要把自動測試當實機驗收。先回覆你讀到的版本、目前任務、三項最高風險、準備修改的檔案，之後直接工作，不要要求我重述歷史。完成前更新 checkpoint、狀態及可上載 GitHub 的完整檔案。

## 附件最小組合

| 目的 | 必須上載 |
|---|---|
| 討論流程／UI | 本文件；相關截圖 |
| 修改程式 | 本文件；最新完整 repository zip；相關截圖 |
| 實機除錯 | 上述全部；裝置、瀏覽器、網址／commit、重現步驟 |
| token 很少 | 本文件；上一 checkpoint；本輪改動檔案 |

手機交付優先使用一個完整 zip；若不能直接改 GitHub，另提供完整替換檔，不只給零散 snippet。

## 專案身份與真相順序

- 產品：磨飯 SMT 餐飲 POS
- 分支：`feat/smt-order-page-v1`
- 程式標籤：`order-v1-12`
- 資料架構：Firebase RTDB 為唯一即時餐牌/API來源；Google Sheet只作記錄投影；Apps Script已退出SMT運行鏈路。
- 目標：先完成可運作點單垂直鏈路
- 自動測試基準：48/48
- 實機：iPad／Sunmi T2S 最終 Lock 未完成

真相順序：當前最新明確確認 → 本文件現行決策 → Current Lock → 最新程式／測試 → 舊 log／效果圖。衝突舊資料標記 `SUPERSEDED`；安全、離線、資料完整及繁中不可跌穿基線。

## 現行決策

### 畫布與卡片

- 頂欄、底部導航、待補及結帳固定；購物車、產品區及卡身各自滾動。
- T2S 是邏輯比例基準；iPad 按安全區適配，可手動縮放。
- 同時只可開一張主卡；新卡先關舊卡；背景、空白不可操作。
- 頂部來源向下開、箭嘴向上；底部來源向上開、箭嘴向下；左源向右；右源向左。

### 購物車與點單

- 可按類別分組；相同配置可合併或逐項。
- 圖片可開關；關閉完全回收位置。
- 名稱清楚放大；金額完全靠右；右側上價，下方固定 `－ 數量 ＋ 修改`。
- 必選完成後為 `結帳 $金額`。
- 普通模式開必選／修改卡；快捷模式直接入車，未完成必選進待補並保持開啟至手動關閉。
- 修改卡顯示產品名、價格、數量、選項及備註；不選等於正常。

### 共用飲品語言

- 快捷飲品平時只顯示導航上方把手「快捷飲品｜待補 N｜⌃」。
- 展開成向上抽屜；最大直向卡，現有目標約抽屜344px、卡150×240。
- 卡名置頂、圖片在下；選中橙框及箭嘴。圖片可關且不留空位。
- 排序、抽屜顯示、圖片、快捷補選是獨立設定。
- 調整預設正常；按「新增調整」才選甜／冰；再按取消；同款可多配置。
- 產品修改的套餐飲品及統一整理飲品沿用同一語言，常駐展開並用較小尺寸。

### 必選、組合與待處理

- Required 阻止結帳；Optional／Link Up 不阻止。
- 指定配對按 `min(可組合主餐數, 小食數)` 建立 A–Z；每組選主餐、小食、飲品，候選每行三張，使用量不可超過購物車。
- 待處理分 App／Web 與電話／WhatsApp，顯示聯絡、件數、金額、等待。
- 核對卡顯示明細、付款及可放大證明；WhatsApp 提供預設訊息 QR。
- 確認付款後才接單；狀態只用 `待處理 → 運行中 → 已完成`，接受30分鐘後完成。

### 其他

- 售罄列表只預覽；設備／線上卡由頂欄來源附近展開。
- 新單提示只在右下約3秒。
- 頂部三點是點單視覺設定；底部更多是低頻後台功能。

## 程式地圖

- `pages/order/page.js`：畫面、卡片、事件、購物車、飲品、配對、待處理。
- `pages/order/page.css`：固定畫布、定位及視覺。
- `pages/order/order-domain.js`：數量、接單、30分鐘完成、WhatsApp link。
- `pages/order/page-data.js`：產品、飲品、待處理示範資料。
- `shared/runtime.js`／`shared/store.js`：本機狀態。
- `app-loader.js`／`app-shell.css`：viewport及縮放。
- `tests/order-edit-flow.test.mjs`：主要回歸。

## 完成度真相

程式及自動測試已有、待實機：購物車右欄、圖片開關、單一卡、快捷／普通點單、飲品抽屜、多配置、A–Z配對、待處理核對、付款證明、WhatsApp QR、30分鐘完成、售罄／設備／線上卡、新單提示。

外部未完成：真實訂單 API、付款證明上載、打印／設備硬件、iPad Safari、Sunmi T2S、產品最終 Lock。實機否定程式時，立即改為「程式有、實機失敗」。

## Chat 模式開發協議

1. 開始：報版本、任務、風險、改動檔案。
2. 工作：小批修改；每批執行語法、全測試及差異檢查。
3. 圖片：先列觀察，再改程式；標註是實機證據。
4. 交付：完整 zip／替換檔、測試及 GitHub 位置。
5. 結束：刷新以下 checkpoint。

```text
SMT CHECKPOINT
branch:
version/commit:
本輪已完成:
本輪未完成:
修改檔案:
測試結果:
實機結果:
下一步唯一任務:
需上載附件:
新／變更決策:
```

## 防止 token 中斷

- 每完成一個可驗證批次即刷新 checkpoint，不等最後。
- 上下文不足時停止擴展新範圍，先保存完整檔案、測試及 checkpoint，再開新 Chat。
- token 不足不是阻止開發的理由；它只觸發「封裝當前批次 → 新 Chat 接力」。
- 新 Chat 只需本文件、最新 zip、最後 checkpoint及有關實機圖。
