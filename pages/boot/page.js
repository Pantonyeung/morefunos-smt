const app=document.getElementById('app');let step=0;const steps=[['裝置授權','輸入工作站及裝置識別，完成一次綁定。','🔐'],['載入本機資料','驗證本機資料庫、菜單、價格及未同步工作。','🗄️'],['準備完成','裝置可離線工作，進入磨飯 SMT 點單。','✅']];
function render(){app.innerHTML=`<div class="app"><header class="topbar"><div class="brand"><span class="logo"></span><span>磨飯 SMT</span></div><div class="top-spacer"></div><span class="muted">啟動與本機載入</span></header><main class="boot-wrap"><section class="boot-card"><div class="boot-title"><span class="logo"></span><h1>磨飯 SMT</h1><p>裝置授權、本機資料載入與離線準備</p></div><div class="steps">${steps.map((s,i)=>`<article class="step-card ${i===step?'active':''}"><div class="step-icon">${s[2]}</div><h2>${s[0]}</h2><p>${s[1]}</p>${i===0?'<div class="device-form"><input value="dev-smt-main" aria-label="裝置"><input value="SMT 主機" aria-label="名稱"></div>':''}${i===1?`<div class="progress ${step>=1?'loading':''}"><span></span></div>`:''}<div class="step-status">${i<step?'已完成':i===step?'進行中':'等待'}</div></article>`).join('')}</div><div class="boot-actions"><button class="btn" data-action="back" ${step===0?'disabled':''}>上一步</button><button class="btn primary" data-action="next">${step===2?'開始點單':'繼續'}</button></div></section></main></div>`;bind()}
function bind(){document.querySelector('[data-action=next]').onclick=()=>{if(step<2){step++;render()}else window.MoreFunPageBridge.navigate('order')};document.querySelector('[data-action=back]').onclick=()=>{if(step>0){step--;render()}}}
render();
