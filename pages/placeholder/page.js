const params=new URLSearchParams(location.search);const label=params.get('label')||'此頁';const route=params.get('page')||'order';document.getElementById('app').innerHTML=`<div class="app"><header class="topbar"><div class="brand"><span class="logo"></span><span>磨飯 SMT</span></div><div class="serial"><small>頁面</small><strong>${label}</strong></div><div class="top-spacer"></div><button class="top-action online">● 線上</button></header><main class="placeholder"><section class="placeholder-card"><span class="logo"></span><h1>${label}頁</h1><p>${label}頁尚未進入本次 SMT-00 至 SMT-04 公網驗收範圍。<br>固定 1920×1080 Page Folder 架構已預留，後續完成後只需更新此分頁資料夾。</p><button class="btn primary" data-route="order">返回點單頁</button></section></main><nav class="bottom-nav"><button data-route="order">點單</button><button class="${route==='orders'?'active':''}" data-route="orders">訂單</button><button class="${route==='dine'?'active':''}" data-route="dine">堂食</button><button class="${route==='supply'?'active':''}" data-route="supply">售罄</button><button class="${route==='more'?'active':''}" data-route="more">更多</button></nav></div>`;
