# feat/smt-order-page-v1 Branch Setup

此資料夾內容應直接放在 GitHub branch 的 repository root，不要再包在另一層 `morefunos-smt-*` 資料夾內。

上載後根目錄應直接看到：

- `index.html`
- `app.js`
- `styles.css`
- `pages/`
- `shared/`
- `docs/`

設計鎖位於：`docs/design-lock-v1/`

現階段程式仍是可運行 V1.4 基線；新點單頁設計功能需要按垂直鏈路逐步實作。
