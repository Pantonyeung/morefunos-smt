window.SMT_API={async save(type,payload){const key="smt_"+type;localStorage.setItem(key,JSON.stringify(payload));return {ok:true,savedAt:new Date().toISOString()}},async load(type,fallback){try{return JSON.parse(localStorage.getItem("smt_"+type))??fallback}catch(e){return fallback}},async sync(){return {ok:true,latency:86,at:new Date().toISOString()}}};